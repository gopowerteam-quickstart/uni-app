export enum DisplayScene {
  List = 'LIST',
  Cover = 'COVER',
  Normal = 'NORMAL',
  Avatar = 'AVATAR',
}

export enum MediaSourceType {
  album = 'ALBUM',
  camera = 'CAMERA',
}

export enum MediaType {
  image = 'IMAGE',
  video = 'VIDEO',
}

export enum BannerType {
  url = 'URL',
  page = 'PAGE',
  project = 'PROJECT',
}
